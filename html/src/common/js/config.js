
export default {
  baseURL: window._ROOT_URL || window.location.origin + '/',
  baseServiceURL: window._SERVER_URL || window.location.origin + '/ide/',
  timeout: 1000 * 20,
  headers: { 'header1': 'header1...' }
}



